# Stacks Network (devnet | testnet | mainnet)
STACKS_NETWORK=testnet

# Wallet — deployer / contract owner address
WALLET_ADDRESS=ST1PQHQKV0RJXZFY1DGX8MNSNYVE3VGZJSRTPGZGM

# Private key — used for on-chain writes (keep secret, never commit)
PRIVATE_KEY=

# OpenAI — powers the AI yield-optimization agent (optional; falls back to deterministic)
OPENAI_API_KEY=

# x402 Micropayment Config
X402_FACILITATOR_URL=https://x402.aibtc.dev
PAYMENT_ADDRESS=ST1PQHQKV0RJXZFY1DGX8MNSNYVE3VGZJSRTPGZGM

# Server
PORT=4000
NODE_ENV=development

# CORS — comma-separated allowed origins (defaults to localhost in dev)
# ALLOWED_ORIGINS=https://your-app.netlify.app,https://yourdomain.com
